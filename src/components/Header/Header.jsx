import Container from "components/Container";
import NavBar from "components/NavBar";

const Header = () => {
  return (
    <Container>
      <NavBar />
    </Container>
  );
};

export default Header;
